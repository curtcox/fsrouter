Add a QR code reader to this app as a new workflow. The change should open the
camera in the browser, display the contents of any detected QR code, and give
the user clear actions for what to do next.

Required behavior:
- Open the camera from a web page in this app.
- Detect QR codes and show the decoded content to the user.
- Let the user choose whether to execute the decoded text, open it as a web
  page in a new tab, open it as a web page in the current tab, say it aloud, or
  use it as a prefilled prompt for an app change.
- Make the UI explain what is happening and handle permissions or unsupported
  browsers cleanly.

Use the AI change workflow to inspect the existing app structure, decide where
the page and handlers belong, decompose the work if needed, review safety, apply
the change, and validate it.
