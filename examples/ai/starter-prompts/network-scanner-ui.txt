Add a network scanner feature to this app. The change should scan the local
network and present the results in a web UI that helps the user understand what
was found.

Required behavior:
- Run a network scan from this machine using tools that are already available or
  can be invoked safely.
- Present discovered machines, discovered services, and any other useful scan
  details in a readable results view.
- Include a topology graph or similar visualization showing what can be inferred
  about the network layout.
- Make the UI explain limitations and partial results when the scan cannot infer
  everything confidently.

Use the AI change workflow to gather the right context, split the work into
smaller parts if needed, review the safety of the implementation, apply the
result, and validate it.
