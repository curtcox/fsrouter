Add a web front end to scheduling on this machine. The change should let the
user inspect current scheduled work and manage it from the browser.

Required behavior:
- Show what is currently scheduled on this machine in a clear, readable view.
- Allow the user to add new scheduled items.
- Allow the user to remove scheduled items.
- Make it clear which scheduling system is being used and what permissions or
  limitations apply.

Use the AI change workflow to discover how this example app is organized,
identify the safest way to interact with scheduling on the host machine,
decompose the work if the AI recommends it, review the changes for safety,
apply them, and validate the result.
